namespace BusinessTests.Services;

public class FileManager_Tests
{
    // Testar att WriteToFile-metoden skriver innehållet till en fil.
    [Fact]
    public void WriteToFile_ShouldWriteToFile()
    {
        // Arrange
        // Act
        // Assert
    }

    // Testar att ReadFromFile-metoden läser innehållet från en fil.
    [Fact]
    public void ReadFromFile_ShouldReadContentFromFile()
    {
        // Arrange
        // Act
        // Assert
    }

    // Testar att FileExists-metoden kontrollerar om en fil existerar.
    [Fact]
    public void FileExists_ShouldCheckIfFileExists()
    {
        // Arrange
        // Act
        // Assert
    }

    // Testar att CreateDirectoryIfNotExists-metoden skapar en mapp om den inte existerar.
    [Fact]
    public void CreateDirectoryIfNotExists_ShouldCreateDirectoryIfNotExists()
    {
        // Arrange
        // Act
        // Assert
    }
}
